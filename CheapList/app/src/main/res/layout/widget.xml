<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout android:id="@+id/widgetLayoutParent"
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:background="@android:color/white">

	<TextView
		android:id="@+id/widgetTitle"
		style="@style/CheapListAppWidget"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:text="@string/app_name" />

	<ListView
		android:id="@+id/widgetShoppingList"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:layout_below="@+id/widgetTitle" />

	<TextView
		android:id="@+id/widgetDefaultMessage"
		android:layout_width="wrap_content"
		android:layout_height="match_parent"
		android:layout_below="@+id/widgetTitle"
		android:layout_centerHorizontal="true"
		android:layout_centerVertical="true"
		android:layout_gravity="center"
		android:text="@string/widget_nothing_to_show"
		android:visibility="gone"
		tools:text="Nothing to show"
		tools:visibility="visible" />
</RelativeLayout>